# NOTE: Replace "srl" with your actual username, or re-generate this file
# with: echo "AllowUsers $USER" | sudo tee /etc/ssh/sshd_config.d/allowusers.conf
# This file is NOT auto-installed by install.sh; copy it manually after editing.
AllowUsers srl
