# Generic PulseAudio defaults — include system configuration.
# Machine-specific sink/source overrides belong in the host overlay:
#   hosts/<hostname>/media/.config/pulse/default.pa
.include /etc/pulse/default.pa
