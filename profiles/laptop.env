# Laptop profile
# Battery management, screen brightness, power saving defaults
INSTALL_PACKAGES_FILE="${INSTALL_PACKAGES_FILE:-} $DOTFILES_DIR/packages/void-laptop.txt"
INSTALL_POWER_PROFILE="${INSTALL_POWER_PROFILE:-1}"
CPU_GOVERNOR="${CPU_GOVERNOR:-powersave}"
CPU_EPP="${CPU_EPP:-power}"
GPU_POWER_LIMIT=""
