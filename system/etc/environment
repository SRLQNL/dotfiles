#
# This file is parsed by pam_env module.
#
# Syntax: simple KEY=VAL pairs on separate lines.
#
EDITOR=nano
BROWSER=xdg-open
# TERMINAL intentionally omitted — set per-host if needed (e.g. foot, kitty, alacritty)
