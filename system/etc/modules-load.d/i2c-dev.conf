# Load i2c-dev for DDC/CI monitor brightness control via ddcutil.
# The module is harmless on machines without DDC/CI-capable monitors,
# so it is kept in the base package rather than a host-specific overlay.
i2c-dev
